$Id$

- when adding audio file take info from ID3 tags
- test searching by tag 
- add "limit" attributes to JSON requests

- port to couchdb 
- make a WSGI script instead of the CGI

- grep -r TODO *

